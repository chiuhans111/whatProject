
/**
 * @param {HTMLCanvasElement} canvas 
 */
function Graph(canvas) {
    var ctx = canvas.getContext('2d');
    var camera = [0, 0, 1];
    var dcamera = [0, 0, 1];
    var tcamera = [0, 0, 1];
    var grid = true;
    var axis = true;
    
}
export default Graph;